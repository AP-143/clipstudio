fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17
websockets==13.1
httpx==0.27.2
pydantic==2.9.2

# AI / ML
faster-whisper==1.0.3
ultralytics==8.3.40
mediapipe==0.10.18
scenedetect[opencv]==0.6.5

# Vision / media
opencv-python-headless==4.10.0.84
Pillow==11.0.0
numpy==1.26.4
ffmpeg-python==0.2.0

# Torch (CUDA 12.1) - installed separately in Dockerfile via extra index,
# pinned here for local/CPU installs
torch==2.4.1
torchvision==0.19.1

# Media download. YouTube now requires solving JS challenges (signature +
# n-challenge) to expose video formats — this needs a recent yt-dlp + the EJS
# solver scripts + a JS runtime (Deno, installed in the Dockerfile).
# yt-dlp is force-upgraded to the latest pre-release in the Dockerfile.
yt-dlp
yt-dlp-ejs>=0.8.0
